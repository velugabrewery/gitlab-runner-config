# gitlab-runner-config

```sh
$ git clone https://github.com/velugabrewery/gitlab-runner-config.git
$ cd gitlab-runner-config
$ cp .env.tempate .env
# Fill env variables
$ sh install.sh
# Log out and back in for this to take effect!
$ sh init.sh
```